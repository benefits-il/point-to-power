# Fixture: clean filesystem handoff

Input: source=filesystem, payload=path to `references/example-handoff.md`.

The fixture is the canonical example-handoff.md as it exists in references/. No outer code fence. Header line is the literal `# PointToPower Handoff v1.0`. Six slides, two NotebookLM recommendations, Visual Queue with 5 entries, Notes To POWER paragraph.

To read the actual fixture content, open `../../../../references/example-handoff.md` from inside the parse-point-handoff skill directory.
