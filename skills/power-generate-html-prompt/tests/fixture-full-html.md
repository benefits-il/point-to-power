# Fixture: full input for generate-html-prompt

Inputs:

- `ast`: the AST from `../parse-point-handoff/tests/expected-output.md`. `meta.target` is `html` natively here.
- `style_record`: Editorial Light from `../select-style/tests/expected-output.md`.
- `layouts`: the 6 layout records from `../write-per-slide-layout/tests/expected-output.md` (CSS Grid vocabulary).
- `image_prompts`: the 5-entry image_prompts list from `../generate-visual-prompts/tests/expected-output.md`.
